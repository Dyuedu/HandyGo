**Chat thời gian thực (Real-time Chat)**

Người dùng mở giao diện Chat

         ↓

Kết nối WebSocket / SocketServer (realtime)

         ↓

Gửi/nhận tin nhắn giữa Customer ↔ Worker (message events)

         ↓

Khi có tin nhắn mới → hiển thị notification; có thể mở cửa sổ chat tương ứng